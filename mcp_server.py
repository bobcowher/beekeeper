#!/usr/bin/env python3
"""
Beekeeper MCP Server — thin shim for running directly from the repo.
Install via pip for the packaged version: pip install beekeeper-mcp
"""
from beekeeper_mcp.server import main

if __name__ == "__main__":
    main()
